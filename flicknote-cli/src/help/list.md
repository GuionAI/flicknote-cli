Examples:
  flicknote list
  flicknote list --project research
  flicknote list --type link
  flicknote list --limit 50
  flicknote list --archived
  flicknote list --json

List output shows the numeric note ID used by detail/content/edit commands.
In JSON output, `id` is the numeric short ID and `uuid` is the full note UUID.
