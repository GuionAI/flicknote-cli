pre-commit:
  parallel: true
  jobs:
    - name: fmt check
      glob: "*.rs"
      run: cargo fmt -- --check

    - name: clippy
      glob: "*.rs"
      run: cargo clippy --all-targets -- -D warnings
