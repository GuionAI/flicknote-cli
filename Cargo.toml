[workspace]
members = [
    "flicknote-core",
    "flicknote-auth",
    "flicknote-cli",
    "flicknote-sync",
]
resolver = "3"

[workspace.dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tokio = { version = "1", features = ["rt", "macros", "time"] }
powersync = { git = "https://github.com/powersync-ja/powersync-native", rev = "81c3c79", features = ["tokio"] }
rusqlite = { version = "0.37", features = ["load_extension", "bundled"] }
http-client = { version = "6.5.3", default-features = false }
async-trait = "0.1"
thiserror = "2"
uuid = { version = "1", features = ["v4"] }
futures-lite = "2"
postgres = { version = "0.19", features = ["with-chrono-0_4"] }

[workspace.lints.rust]
unsafe_code = "warn"
unreachable_pub = "warn"

[workspace.lints.clippy]
# Error swallowing prevention
let_underscore_must_use = "warn"
let_underscore_untyped = "warn"

# Prevent debug artifacts in production
dbg_macro = "warn"
print_stdout = "warn"
print_stderr = "warn"

# Useful pedantic subset (low noise, high value)
needless_pass_by_value = "warn"
redundant_closure_for_method_calls = "warn"
use_self = "warn"
